# LARK ENTRY — public template

This file is a **template to copy into your private runtime repository**.

For real use, Chat should first read the private copy of this file through the connected GitHub integration. Do not use a public Lark/Feishu webpage as a substitute for the authenticated bridge.

Then read:

`bridge/GLOBAL_CHAT_ROUTING.md`

and follow that routing/security protocol.

Recommended first-hop template after deployment:

```text
LARK BRIDGE: Your first tool call must use the connected GitHub repository to read YOUR_ACCOUNT/YOUR_PRIVATE_RUNTIME_REPO, branch runtime, file bridge/LARK_ENTRY.md. Do not open, search, or browse any Lark/Feishu webpage. After reading the entry file, follow its routing and safety protocol for the task below.
```

This public template contains no live mailbox state or OAuth material.
